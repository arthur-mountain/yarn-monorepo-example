"use client";
import { Button } from "@yarn-monorepo-example/components";

export default function Demo() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      Demo
      <Button label="demo" onPress={() => {}} />
    </main>
  );
}
